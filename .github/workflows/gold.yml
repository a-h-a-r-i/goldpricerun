name: DP Gold Telegram Alert

on:
  schedule:
    - cron: "30 4 * * *"
    - cron: "30 11 * * *"
  workflow_dispatch:

jobs:
  gold:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm install

      - run: node index.js
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
